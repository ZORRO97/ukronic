<nav class="fh5co-nav" role="navigation">
    <div class="container">
      <div class="row">
        <div class="left-menu text-right menu-1">
          <ul>
            <li><a href="{{ path('main') }}">ACCUEIL</a></li>
            <li><a href="{{ path('search') }}">FILMS</a></li>
            <li><a href="{{ path('searchSerie') }}">SERIES</a></li>
            <li>
              {% if is_granted("ROLE_USER") %}
            <a href="{{ path('profile') }}">PROFIL</a>
          {% endif %}
              
            </li>
          </ul>
        </div>
        <div class="logo text-center">
          <div id="fh5co-logo"><img src="{{ asset("Ukronic-logo-blanc-web.png") }}" alt="" width=150px height=75px></div>
        </div>
        <div class="right-menu text-left menu-1">
          <ul>
            
            <li class="has-dropdown">
              <a class="dropdown-toggle" role="button" data-toggle="dropdown" href="#"><i class="glyphicon glyphicon-user"></i> 
              {% if app.user.username is defined %}
                {{ app.user.username }}
              {% else %}
                Anonyme      
              {% endif %} 
              <span class="caret"></span></a>
            <ul id="g-account-menu" class="dropdown-menu" role="menu">
            <li>
              <a href="#">
                {% if is_granted("ROLE_USER") %}
                  {{ app.user.roles[0] }}
                 {% else %}
                      Aucun droit 

                {% endif %} 
              </a>
            </li>
            </ul>
            </li>
            <li>
              {% if is_granted("ROLE_USER") %}       
                <a href="{{ path('fos_user_security_logout') }}"><i class="glyphicon glyphicon-lock"></i>
                Logout
                </a>
              {% else %}
                <a href="{{ path('fos_user_security_login') }}"><i class="glyphicon glyphicon-lock"></i>
                  Login
                </a>   
              {% endif %}
            </li>
            <li>
              {% if not is_granted("ROLE_USER") %}       
                <a href="{{ path('fos_user_registration_register') }}"><i class="glyphicon glyphicon-lock"></i>
                Inscription
                </a>
                 
              {% endif %}
            </li>
            
          </ul>
        </div>
      </div>
      
    </div>
  </nav>
  <header id="fh5co-header" class="fh5co-cover" role="banner" style="background-image:url({{ asset('images/img_bg_2.jpg') }});">
    <div class="overlay"></div>
    
  </header>

<!-- Header -->
{#
<div id="top-nav" class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
      </button>
      
    </div>
    <div class="navbar-collapse collapse ukronic">
      <ul class="nav navbar-nav navbar-left">
        <li><img src="{{ asset("Ukronic-logo-blanc-web.png") }}" alt="" width=300px height=155px></li>
        <li class="dropdown"><a href="{{ path('search') }}">FILMS</a></li>
        <li class="dropdown"><a href="{{ path('searchSerie') }}">SERIES</a></li>

        <li>
          {% if is_granted("ROLE_USER") %}
            <a href="{{ path('profile') }}">Mon compte</a>
          {% endif %}
        </li>
        
        <li class="dropdown">
          <a class="dropdown-toggle" role="button" data-toggle="dropdown" href="#"><i class="glyphicon glyphicon-user"></i> 
          {% if app.user.username is defined %}
            {{ app.user.username }}
          {% else %}
            Anonyme      
          {% endif %} 
          <span class="caret"></span></a>
          <ul id="g-account-menu" class="dropdown-menu" role="menu">
            <li>
              <a href="#">
                {% if is_granted("ROLE_USER") %}
                  {{ app.user.roles[0] }}
                 {% else %}
                      Aucun droit 

                {% endif %} 
              </a>
            </li>
          </ul>
        </li>
        <li>
            {% if is_granted("ROLE_USER") %}       
            <a href="{{ path('fos_user_security_logout') }}"><i class="glyphicon glyphicon-lock"></i>
            Logout
            </a>
            {% else %}
             <a href="{{ path('fos_user_security_login') }}"><i class="glyphicon glyphicon-lock"></i>
            Login
            </a>   
            {% endif %}
         </li>
      </ul>
    </div>
  </div><!-- /container -->
</div>
<!-- /Header -->
#}