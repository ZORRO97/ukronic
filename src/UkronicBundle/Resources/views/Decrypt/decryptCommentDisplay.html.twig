{% if decrypt is not null  %}
a commenté la{% if decrypt.typeDecrypt == "S" %} séquence {% else %} fin {% endif %} {{ decrypt.title }} pour {{ decrypt.movie.title }}
{% else %}
	a commenté un décryptage supprimé par l'administrateur
{% endif %}
